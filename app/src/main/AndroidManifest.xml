<manifest xmlns:android="http://schemas.android.com/apk/res/android"

          package="com.bukinmm.mobiledevelopertesttask"
    >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <uses-permission android:name="android.permission.INTERNET" />

        <activity android:name=".MainActivity">
        <!--<activity android:name=".PropertyListActivity">-->
        <intent-filter>
                <action android:name="android.intent.action.MAIN"></action>
                <category android:name="android.intent.category.LAUNCHER"></category>
            </intent-filter>

        </activity>
        <!--<activity android:name=".MainActivity"></activity>-->
        <activity android:name=".PropertyActivity"></activity>
        <activity android:name=".PropertyListActivity"></activity>
        <activity android:name=".EditObjectActivity"></activity>
        <activity android:name=".AddNewActivity"></activity>
    </application>

</manifest>
